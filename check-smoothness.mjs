import {createRequire} from 'node:module';const require=createRequire(import.meta.url);const {chromium}=require('C:/Users/user/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const browser=await chromium.launch({headless:true,channel:'msedge',args:['--enable-webgl','--ignore-gpu-blocklist']});try{const page=await browser.newPage({viewport:{width:1440,height:960}});await page.goto('http://127.0.0.1:5173/');await page.waitForFunction(()=>window.__silva?.frames>40);await page.locator('#walk-start').click();await page.waitForFunction(()=>__silva.walker.locked);await page.keyboard.down('w');console.log(await page.evaluate(()=>new Promise(resolve=>{const samples=[];let previous=performance.now();const start=previous;function sample(now){samples.push(now-previous);previous=now;if(now-start<3500)requestAnimationFrame(sample);else{samples.sort((a,b)=>a-b);resolve({fps:1000/(samples.reduce((a,b)=>a+b,0)/samples.length),p95:samples[Math.floor(samples.length*.95)],over33ms:samples.filter(t=>t>33.3).length});}}requestAnimationFrame(sample);})));}finally{await browser.close();}
